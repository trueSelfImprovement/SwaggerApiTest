server:
  port: 6868
spring:
  application:
    name: swagger-eureka
  rabbitmq:
    host: localhost
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/